<div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
			<%= link_to "BOSO", root_path, :class => 'brand' %>
            <ul class="nav">
			  <% Tag.navigation_tags.each do |tag| %>			  	
				  <% if params[:tag] == tag.name %>
				  	  <li class="active"><%= link_to tag.name, tag.name %></li>
				  <% else %>
					  <li><%= link_to tag.name, tag.name %></li>
				  <% end %>
			  <% end %>
			</ul>
        </div>
      </div>
   </div>