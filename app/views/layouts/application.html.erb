<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Boso" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Boso" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
      <div class="container-narrow">
        <div class="masthead">
	          <%= render 'layouts/navigation' %>
          <h3 class="muted"><%= link_to "BOSO", root_path, :class => 'brand' %></h3>
        </div>

        <hr>

        <div class="jumbotron">
            <%= render 'layouts/messages' %>
            <%= yield %>			
        </div>

        <hr>

        <div class="footer">
			© Built by <a href="http://5kmvp.com">5KMVP</a>
        </div>

      </div> <!-- /container -->	  	  
  </body>
</html>
