<%= simple_form_for(@tag) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :num_questions %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
